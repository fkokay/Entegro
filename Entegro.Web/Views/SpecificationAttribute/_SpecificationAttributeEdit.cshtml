@model SpecificationAttributeModel

<div class="row g-3 p-3">
    <input type="hidden" value="@Model.Id" asp-for="Id" />

    <div class="col-md-12">
        <label class="form-label text-sm-start" for="Name">Adı</label>
        @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
    </div>
</div>