@model BrandModel
@using System.Text.Encodings.Web
@{
    ViewData["Title"] = "Marka bilgilerini düzenle - " + Model.Name;
}

<div class="container-fluid flex-grow-1 container-p-y">
    <form asp-action="Edit" id="brand-form">
        <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center mb-6 row-gap-4">
            <div class="d-flex flex-column justify-content-center">
                <h4 class="mb-1">Marka bilgilerini düzenle</h4>
                <p class="mb-0">Marka bilgilerini düzenle</p>
            </div>
            <div class="d-flex align-content-center flex-wrap gap-4">
                <div class="d-flex gap-4">
                    <a href="@Url.Action("List")" class="btn btn-label-secondary d-grid w-100 waves-effect">
                        <span class="d-flex justify-content-center text-nowrap">
                            <i class="ti ti-circle-x ti-xs me-1"></i> Vazgeç
                        </span>
                    </a>
                    <button type="button" id="btnDeleteBrand" data-id="@Model.Id" class="btn btn-danger d-grid w-100 waves-effect waves-light">
                        <span class="d-flex justify-content-center text-nowrap">
                            <i class="ti ti-trash ti-xs me-1"></i> Sil
                        </span>
                    </button>
                </div>
                <button type="submit" class="btn btn-primary waves-effect waves-light">
                    <span class="d-flex justify-content-center text-nowrap">
                        <i class="ti ti-device-floppy ti-xs me-1"></i> Kaydet
                    </span>
                </button>
            </div>
        </div>

        <div class="row">
            <partial name="_CreateOrUpdate" />
        </div>
    </form>
</div>

@section Scripts{
    <script src="/assets/js/entegro.brand.js"></script>
    <script>
        $(function () {
            Entegro.brand.init('/brand/edit');
        });
    </script>


}